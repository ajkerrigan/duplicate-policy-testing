policies:
  - name: aws-security-group-list
    resource: aws.security-group
    filters:
      # - not:
      #   - type: ingress
      #     Cidr:
      #       value_type: cidr_size
      #       op: greater-than
      #       value: 32
      # key: "IpPermissions[].IpRanges[].CidrIp"
      # op: less-than
      # value_type: cidr_size
      # value: 31
    # ...
    # - type: ingress
    #   IpProtocol: "-1"
    - or:
          - type: ingress
            Ports: [22]
          - type: ingress

            match-operator: or
            Ports: [22]
            Cidr: 67.85.224.13/32
          - type: ingress
            Cidr: 0.0.0.0/0
            # Cidr:
            #   value: "0.0.0.0/0"
          # - type: ingress
          #   Ports: [22]
          #   CidrV6:
          #     value: "::/0"
